interface:
  display_name: "Code Quality Audit"
  short_description: "Review risky fallback and masking patterns"
  default_prompt: "Use $code-quality-audit to review the changed files and classify suspicious fallback patterns."
policy:
  allow_implicit_invocation: true
