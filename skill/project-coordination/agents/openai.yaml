interface:
  display_name: "Project Coordination"
  short_description: "Coordinate multi-component delivery work"
  default_prompt: "Use $project-coordination to triage, dispatch, or accept this project work."
policy:
  allow_implicit_invocation: true
