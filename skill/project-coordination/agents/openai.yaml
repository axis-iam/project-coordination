interface:
  display_name: "Project Coordination"
  short_description: "Plan and coordinate multi-component delivery"
  default_prompt: "Use $project-coordination to plan, dispatch, or accept this project work."
policy:
  allow_implicit_invocation: true
